#include<iostream>
using namespace std;
int main()
{
    int n,i,flag,j;
    cin>>n;
    for(i=2;i<=n;i++)
    {
        flag=0;
        for(j=2;j<=(n/2);j++)
        {
            if(j!=i)
            {if(i%j==0)
                flag=1;
        }
        }
        if(flag==0)
            cout<<i<<" ";
        else
            continue;
    }
}
